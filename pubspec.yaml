name: films_app_practie
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  equatable: ^2.0.3
  bloc: ^8.0.2
  flutter_bloc: ^8.0.1
  dio: ^4.0.4
  get_it: ^7.2.0
  url_launcher: ^6.0.20
  share_plus: ^3.0.4
  http: ^0.13.4
  path_provider: ^2.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  test: ^1.19.5

flutter:
  uses-material-design: true